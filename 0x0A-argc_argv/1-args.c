#include <stdio.h>
#include <stdlib.h>
/**
* main - Entry point
* @argc: incoming argument
* @argc: incoming argument
*
* Return: Always 0 (Success)
*/
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc-1);
return (0);
}
