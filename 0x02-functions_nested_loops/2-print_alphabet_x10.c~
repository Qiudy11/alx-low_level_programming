#include "holberton.h"
void print_alphabet_x10(void)
{
int i = 0, j;
  while (i<10)
    {
    j = 0;
    while(j<1)
    {
 for(j = 0; j < 26; ++j)
    _putchar('a' + j);
    {
    _putchar('\n');
    }
    }
        _putchar('\n');
        i++;
  }
  return (0);
}
